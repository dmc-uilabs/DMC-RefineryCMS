<%= render 'refinery/admin/search_header', :url => refinery.organizations_admin_addresses_path %>
<div class='pagination_container'>
  <% if @addresses.any? %>
    <%= render 'addresses' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
