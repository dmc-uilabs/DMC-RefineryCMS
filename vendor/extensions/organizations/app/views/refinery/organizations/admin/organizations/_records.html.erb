<%= render 'refinery/admin/search_header', :url => refinery.organizations_admin_organizations_path %>
<div class='pagination_container'>
  <% if @organizations.any? %>
    <%= render 'organizations' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
